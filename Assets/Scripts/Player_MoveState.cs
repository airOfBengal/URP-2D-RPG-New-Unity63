using UnityEngine;

public class Player_MoveState : EntityState
{
    public Player_MoveState(Animator anim, StateMachine stateMachine, string stateName) : base(anim, stateMachine, stateName)
    {
    }
}
